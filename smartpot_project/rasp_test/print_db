import sqlite3

conn = sqlite3.connect("local_data.db")
cursor = conn.cursor()

cursor.execute("SELECT * FROM settings")
rows = cursor.fetchall()

for row in rows:
    print(row)

conn.close()